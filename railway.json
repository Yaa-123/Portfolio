{
  "builds": [
    {
      "name": "client",
      "builder": "nixpacks",
      "buildDir": "client",
      "buildCommand": "npm install && npm run build",
      "startCommand": "npx serve -s build -l $PORT"
    }
  ]
}
